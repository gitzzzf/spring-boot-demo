server:
  port: 8081
spring:
  datasource:
    name: test
    url: jdbc:mysql://127.0.0.1:3306/quartz
    username: root
    password:
  redis:
    host: 127.0.0.1
    port: 6379
    password:
    jedis:
      pool:
        max-active: 10
        max-total: 20
        max-idle: 8
        min-idle: 0
        max-wait: 1000
        test-on-borrow: false








